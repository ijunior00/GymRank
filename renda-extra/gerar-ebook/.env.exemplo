# Copie este arquivo para ".env" (sem o ".exemplo") e cole a sua chave.
# O ".env" está no .gitignore: nunca sobe para o GitHub. Nunca cole a chave
# em conversas nem em outros arquivos.
ANTHROPIC_API_KEY=cole-aqui-a-sua-chave
